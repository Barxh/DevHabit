name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: "10.x"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: DevHabit

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore DevHabit.sln

      - name: Build
        run: dotnet build DevHabit.sln -c Release --no-restore

      - name: Test
        run: dotnet test DevHabit.sln -c Release --no-restore --no-build

      - name: Publish
        run: dotnet publish DevHabit.sln -c Release --no-restore --no-build
